🧠 Архитектура мультиагентного AI-ассистента для автосалона

1. Клиент взаимодействует через:
   • Веб-виджет (Voiceflow)
   • Telegram-бот
   • VK-страницу

2. Входящее сообщение попадает в Flask-сервер, где работают три агента:

🔹 Агент 1 — Refiner  
• Переформулирует запрос клиента (например: “что-то стучит в подвеске”)  
• Генерирует гипотезу для уточнения  
• Передаёт запрос дальше  

🔹 Агент 2 — Retriever  
• Осуществляет поиск по FAISS (услуги, модели, запчасти)  
• Подтягивает данные из JSON-базы (адреса, графики, цены)  
• Возвращает найденные блоки  

🔹 Агент 3 — Planner  
• Получает всё от Refiner и Retriever  
• Составляет итоговый ответ  
• Общается с клиентом  
• Предлагает запись, уточняет удобное время и филиал  
• Возвращает результат через Flask → Voiceflow / Telegram / VK

3. Fallback и логирование:
• Make → логирует диалоги в Google Sheets  
• Отправляет уведомление менеджеру в Telegram (если нужно вмешательство)

🧰 Стек:
• OpenAI GPT (внутри агентов)
• FAISS база (10 000+ записей)
• JSON база (адреса, цены, графики)
• Flask (сервер и логика агентов)
• LangChain (цепочки)
• Make (логирование, уведомления)
• Voiceflow, Telegram, VK
• Кэширование часто задаваемых запросов
